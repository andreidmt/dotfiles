#!/bin/sh

DAY=$(date +%-e)

case $DAY in
  1?) DAY=${DAY}th ;;
  1)  DAY=${DAY}st ;;
  2)  DAY=${DAY}nd ;;
  3)  DAY=${DAY}rd ;;
  *)  DAY=${DAY}th ;;
esac

printf '%s\n' "$(date "+%A, $DAY %B (%m) %Y, %T ")" 
