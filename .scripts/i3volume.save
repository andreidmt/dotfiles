#!/bin/bash

case $BLOCK_BUTTON in
	1) $TERMINAL -e pulsemixer ;;
	3) pamixer -t ;;
	4) pamixer -i 5 ;;
	5) pamixer -d 5 ;;
esac

volume=$(pamixer --get-volume)%
isMute=$(pamixer --get-mute)

if [ $isMute = true ]; then
	echo " MUTE $volume";
	exit 33;
else
	echo " VOL $volume";
fi


